---
description: harness-kit 환경 진단 — 필수 도구/설치 파일/훅 권한 체크리스트 출력
---

harness-kit 설치 환경을 진단합니다. 다음 명령을 실행하고 결과를 사용자에게 그대로 출력하세요:

```bash
bash .harness-kit/bin/sdd doctor
```

출력 후 추가 설명 없이 결과를 그대로 보여줍니다.
FAIL 항목이 있으면 각 항목 아래 해결 안내가 이미 포함되어 있으므로 중복 설명하지 않습니다.
